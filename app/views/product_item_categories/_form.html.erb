
<%= form_with(model: product_item_category, local: true, class: "uk-form-horizontal") do |form| %>

  <% if product_item_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_item_category.errors.count, "error") %> prohibited this product_item_category from being saved:</h2>

      <ul>
      <% product_item_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="uk-margin-small">
    <div class="uk-grid">
      <div class="uk-width-1-4@m">Name</div>
      <div class="uk-width-3-4@m">
        <%= form.text_field :name, id: :product_item_category_name, :class=> "uk-input uk-form-width-large uk-form-small" %>
      </div>
    </div>
  </div>

  <hr>
  <p uk-margin>
    <%= link_to 'Back', product_item_categories_path, class: "uk-button uk-button-secondary uk-button-small" %>
    <%= form.submit "Save", class: "uk-button uk-button-primary uk-button-small" %>
  </p>

<% end %>
