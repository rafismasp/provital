<%= form_with(model: delivery_driver, local: true) do |form| %>
  <% if delivery_driver.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(delivery_driver.errors.count, "error") %> prohibited this delivery_driver from being saved:</h2>

      <ul>
      <% delivery_driver.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :delivery_driver_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
