
<div class="uk-container uk-container-expand">        
  <div class="uk-grid uk-grid-medium  " data-uk-grid>
    <div class="uk-width-1-2@m">
      <div class="uk-card uk-card-default uk-card-small uk-card-hover">
        <div class="uk-card-header">
          <div class="uk-grid-small uk-flex-middle" uk-grid>
            <div class="uk-width-auto">
              <% if current_user.avatar.url.present? %>
                <%= image_tag current_user.avatar.url, :class=> "uk-border-circle", :alt=> "", :height=> 40, :width=> 40 %>
              <% else %>
                <%= image_tag "user.svg", :class=> "uk-border-circle", :alt=> "", :height=> 40, :width=> 40 %>
              <% end %>
            </div>
            <div class="uk-width-expand">
              <h3 class="uk-card-title uk-margin-remove-bottom"><%= current_user.first_name %></h3>
              <p class="uk-text-meta uk-margin-remove-top"><%= current_user.department.name if current_user.department.present? %></p>
            </div>
          </div>
        </div>
        <div class="uk-card-body">
          <form class="uk-form-horizontal">
            
            <div class="uk-flex">
              <div class="uk-width-1-3 uk-padding-small uk-text-bold">Jadwal Kerja</div>
              <div class="uk-width-1-3"></div>
              <div class="uk-width-1-3 uk-padding-small uk-text-right"><%= @employee_presence.date.strftime("%A, %d-%B-%Y") %></div>
            </div>

            <div class="uk-flex uk-flex-center">
              <div class="uk-text-lead uk-text-bold"><%= current_user.current_employee_schedule %></div>
            </div>
            
            <div class="uk-flex uk-flex-center">
              <div class="uk-text-muted">Office Hours</div>
            </div>
            <hr>
            <div class="uk-flex uk-flex-center">
              <div>Clock IN: <%= @employee_presence.clock_in.to_datetime.strftime("%Y-%m-%d %H:%M:%S") if @employee_presence.clock_in.present?%></div>
              <div class="uk-width-1-5"></div>
              <div>Clock Out: <%= @employee_presence.clock_out.to_datetime.strftime("%Y-%m-%d %H:%M:%S") if @employee_presence.clock_out.present?%></div>
            </div>

            <hr>
            <p uk-margin>
              <%= link_to 'Back', employee_presences_path, class: "uk-button uk-button-secondary uk-button-small" %>
              <%= link_to 'Presensi', edit_employee_presence_path(@employee_presence), class: "uk-button uk-button-primary uk-button-small" %>
            </p>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>