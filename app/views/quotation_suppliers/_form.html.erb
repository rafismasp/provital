
<%= form_with(model: quotation_supplier, local: true, class: "uk-form-horizontal") do |form| %>

  <% if quotation_supplier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quotation_supplier.errors.count, "error") %> prohibited this quotation_supplier from being saved:</h2>

      <ul>
      <% quotation_supplier.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="uk-margin-small">
    <div class="uk-grid">
      <div class="uk-width-1-4@m">Number</div>
      <div class="uk-width-1-3@m">
        <%= form.text_field :number, id: :quotation_supplier_number, :class=> "uk-input uk-form-width-large uk-form-small" %>
      </div>
    </div>
  </div>

  <hr>
  <p uk-margin>
    <%= link_to 'Back', quotation_suppliers_path, class: "uk-button uk-button-secondary uk-button-small" %>
    <%= form.submit "Save", class: "uk-button uk-button-primary uk-button-small" %>
  </p>

<% end %>
